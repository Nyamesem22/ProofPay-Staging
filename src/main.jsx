import React from "react";
import { createRoot } from "react-dom/client";
import { MobileExact } from "./MobileExact.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MobileExact />
  </React.StrictMode>,
);
